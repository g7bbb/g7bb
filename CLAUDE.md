# 곤충 배틀 베타 (곤충진화 ai)

## 이 프로젝트가 뭔지

곤충체험 사업에서 쓸 체험용 웹앱의 베타 버전. 아이들이 A4 용지에 곤충을 그리고 필요한 정보를 표시한 뒤,
그 종이를 사진으로 찍어 앱에 올리면 → AI가 멋진 곤충 이미지로 변환 → 다른 아이 곤충과 환경별 생존 배틀 → 랭킹 확인.

**일정**: 9월 말까지 기능 완성 → **10월 3~4일(2일간)** 대전 곤충행사에서 현장 테스트.
**예상 인원**: 2일간 약 300명.
**목표 수준**: 디자인 완성도보다 "체험을 실제로 한두 번 돌려볼 수 있는" 베타 퀄리티.

## 작업 순서 (2026-09-17 확정) — 이 순서대로만 진행할 것

작업이 뒤죽박죽 진행된다는 지적이 있었음. **아래 순서를 벗어나지 말 것.**
새 아이디어가 나오면 "사용자 요청 사항"에 적어만 두고, 순서는 건드리지 않는다.

| # | 작업 | 담당 | 기한 | 상태 |
|---|---|---|---|---|
| 0 | Supabase 마이그레이션 실행 | Jin | 즉시 | ✅ 완료 (2026-09-17, ticket/survey + mutations 둘 다) |
| 1 | g7bb.vercel.app 폰으로 테스트 | Jin | 즉시 | 🔄 진행 중 |
| 2 | 종이 레이아웃 확정 | Jin | 9/18 | ✅ **완료** (2026-09-18). 곤충 종류 6개 + 색깔·느낌 2줄까지 확정. 바로 뽑으면 됨 |
| 3 | 인쇄용 종이 | Claude | 9/18 | ✅ `/print` 페이지로 구현 (브라우저에서 PDF 저장). 곤충 종류 확정되면 그대로 뽑으면 됨 |
| 4 | 구글시트 Apps Script 설정 | Jin | 9/21 | ⬜ |
| 5 | 예쁜 곤충 심사 관리자 화면 | Claude | 9/23 | ⬜ (심사 방식 결정 대기) |
| 6 | OMR 마킹 자동 인식 | Claude | 9/27 | ⬜ |
| 7 | 실물 종이로 처음부터 끝까지 통합 테스트 | 둘 다 | 9/30 | ⬜ |
| 8 | 예비일 (밀린 것 정리) | | 10/1~2 | |

**인쇄는 타임라인에서 제외** (2026-09-18 사용자 요청). Jin이 인쇄샵에서 직접 진행하므로
일정 관리·알림 대상이 아님. `/print` 페이지는 준비돼 있으니 곤충 종류만 확정되면 언제든 뽑으면 됨.

**순서를 이렇게 잡은 이유**

- **0~1번이 최우선**: 마이그레이션 전에는 앱이 아무것도 저장하지 못한다. 지금 막혀 있는 상태.
- **2번(곤충 종류)은 종이를 뽑기 전에 정해져야 했다** → 2026-09-18 확정 완료. 이제 `/print`에서
  바로 뽑으면 된다. 인쇄 일정은 Jin이 직접 관리한다.
- **6번(OMR)이 마지막인 이유**: 실패해도 앱에서 직접 입력하면 체험은 돌아간다. 유일하게 포기 가능한 항목.

## 사용자와 협업하는 방식 (중요)

**말투 (2026-09-18 본인 요청)**: 사용자 이름은 **Jin**. **반말로, 편하게** 대화한다.
"사장님"·"주인님" 같은 호칭은 쓰지 않는다. ("사장은 너무 먼 것 같아", "걍 편하게 말하자")
→ 설명이 편해졌다고 대충 하라는 뜻은 아니다. 초보자 눈높이로 꼼꼼히 설명하는 건 그대로 간다.
→ 알림(PushNotification)과 정기 점검 문구도 같은 말투로 맞춘다.

- 사용자는 개발 지식이 거의 없음. **콘텐츠·방향·결과에 대한 피드백**을 담당.
- **개발 결정과 효율적인 방법은 Claude가 정하고, 초보자 눈높이의 한국어로 설명**할 것.
- 계정 생성·로그인·결제처럼 사용자 본인이 해야 하는 일은 클릭 순서까지 구체적으로 안내.
- 게임 밸런스·이미지 스타일 같은 콘텐츠 결정은 기본안을 제시하고 피드백을 받는 방식.
- **틀린 건 틀렸다고 말한다.** 지금까지 이게 도움이 됐다 (나비 날개 보너스 구멍, 종이가
  까맣게 인쇄되던 버그 둘 다 이렇게 잡았다). 말투가 편해져도 이건 유지.

## 기술 스택과 선택 이유

| 기술 | 이유 |
|---|---|
| Next.js 14 (App Router) + TypeScript | 화면과 서버 기능을 한 프로젝트에서 처리. 별도 백엔드 서버 불필요 |
| Vercel | 배포 대상 (아직 미배포). GitHub 연동만 하면 자동 배포 |
| Supabase | 아이디/곤충/배틀 기록 저장. 브라우저에서 직접 호출하는 단순 구조 |
| Google Gemini 이미지 API | 그림 → 곤충 이미지 변환. **서버(API route)에서만 호출**해 키 노출 방지 |
| Tailwind CSS | 최소한의 스타일링으로 빠르게 모바일 화면 구성 |

## 4가지 핵심 기능 상태

1. **사진 → 곤충 이미지 변환** — 작동 확인 완료 (`app/api/convert-insect/route.ts`)
2. **환경별 생존 배틀** — 작동 확인 완료. 랭킹에서 상대 고르기, VS 화면, 타격/피격 이펙트,
   배틀당 1번 필살기, HP 바, 크리티컬 포함 (아래 "배틀 연출 강화" 참고)
3. **아이디 생성 / 랭킹** — 작동 확인 완료
4. **카카오톡 채널 링크** — 코드는 있음. `NEXT_PUBLIC_KAKAO_CHANNEL_URL` 값만 넣으면 됨

## 게임 시스템 설계 (사용자가 직접 정한 규칙)

**5개 핵심 능력치**: 공격력(atk) · 수비력(def) · HP(hp) · 생존능력(surv) · 지능(int)

**7개 신체부위(각 1~5점) → 능력치 매핑** (`lib/insect-stats.ts`):

| 부위 | 연결되는 능력치 |
|---|---|
| 머리(턱) | 공격력, 지능 |
| 다리(발톱) | 공격력, 생존능력 |
| 갑옷(두께) | HP, 수비력 |
| 날개 | 수비력, 생존능력 |
| 눈 | 수비력, 생존능력 |
| 육감 | 공격력, 수비력, 생존능력, 지능 |
| 유전 | 공격력, 수비력, 생존능력 |

각 능력치는 기여하는 부위 점수 합을 0~100으로 정규화. **수비력만 성장단계 보정을 받지 않음.**

**성장단계**: 신생충(0.9배) → 1년충(1.0배) → n년충(1.15배). 단계가 높을수록 경험치도 더 많이 획득.

**환경별 가중치** (`lib/battle-engine.ts`) — 같은 곤충도 환경에 따라 결과가 달라지도록 Claude가 설계한 부분:
운석충돌(생존·수비·HP) / 기온상승(생존·지능·수비) / 저지대(공격·수비·생존) / 물근처(생존·수비·HP) / 고지대(지능·생존·HP)

**레벨/크리티컬**: XP 50당 1레벨. 레벨당 능력치 +3%, 크리티컬 확률 +2%(기본 10%, 최대 50%). 크리티컬 시 점수 1.5배.

밸런스 조정이 필요하면 위 두 파일의 숫자만 바꾸면 됨.

## 사용자 요청 사항 (기억할 것)

- **시작 시 선물 선택 기능**: 아이가 게임을 처음 시작할 때 "받고 싶은 선물"을 고르게 하는 옵션.
  랭킹 상위 입상 시 그 선물을 지급하는 구조. 동기부여 + 어떤 선물이 인기인지 실시간 파악 용도.
  → `players`에 `prize_choice` 컬럼 추가 필요.

- **간단한 설문** (문항 확정됨 2026-09-17, 형식은 선택형 권장안 제시 후 승인 대기):
  1. 제일 좋아하는 곤충 2. 선물 중 받고 싶은 상품 3. 채집 체험 해보고 싶은지
  4. AI 곤충배틀 게임이 나오면 해보고 싶은지
  → 2번은 위 "선물 선택 기능"과 같은 항목이므로 하나로 합칠 것.

## ✅ 특별 진화: 상상으로 더 그린 부위 (2026-09-17 확정·구현 완료)

**배경**: 생성된 왕사슴벌레의 큰턱이 4개로 나왔다. 처음에는 종별 해부학 규칙을 넣어
실제대로만 그리게 고쳤으나, 사용자가 뒤집어 생각했다 — 이 체험은 도감이 아니다.
아이 그림을 AI가 "고쳐"버리면 "내가 그린 거랑 다르다"가 되어 체험의 핵심이 깨진다.

**확정된 방식(B안)**: 상상대로 그려주되, **얻는 만큼 잃게** 한다. `lib/mutations.ts`

| 아이가 그린 것 | 얻는 것 | 잃는 것 |
|---|---|---|
| 큰턱 2→3→4개 이상 | 공격력 +12%/단계 | 생존 −8%, 지능 −8% |
| 날개 2→3→4장 이상 | 수비력 +12%/단계 | HP −8% |
| 다리 6→7→8개 이상 | 생존능력 +12%/단계 | 공격력 −8% |
| 더듬이 2→3개 이상 | 지능 +18% | 수비력 −10% |

- 밸런스 숫자는 `lib/mutations.ts` 한 파일에서만 고치면 된다.
- 이미지 프롬프트에 "아이의 상상이므로 절대 실제 개수로 고치지 마라"를 명시했다.
- `lib/species.ts`의 해부학 규칙은 **아이가 개수를 지정하지 않은 부위에만** 적용된다.
- 종이에 이 4줄 마킹란이 들어가야 한다 (순서표 2번).

⚠️ **"정상 개수"는 종마다 다르다 (2026-09-18 수정)**

나비·벌·사마귀는 **원래 날개가 4장**이다. 처음 코드는 모든 곤충의 정상 날개를 2장으로 봤기 때문에,
나비를 그린 아이가 사실대로 4장이라고 표시하면 **가만히 있어도 수비력 +12%**를 받는 상태였다.
상품이 걸린 랭킹이라 그냥 둘 수 없어 고쳤다.

- 종별 기준값은 `lib/species.ts`의 `normalWings`에 있다. (나비·벌·사마귀 4, 나머지 2)
- 날개 선택지를 `2장 / 3장 / 4장 이상` → **`2장 / 4장 / 6장 이상`**으로 바꿨다.
  실제 날개는 한 쌍씩 늘고, 나비의 정상값 4장이 선택지 안에 있어야 하기 때문이다.
- 단계는 개수를 빼서 세지 않고 **선택지 순서**로 센다(`stepsAbove`). 2→4가 두 단계로 계산되는 것을 막는다.
- `calculateStats(parts, ageStage, mutations, species)` 처럼 **species를 같이 넘겨야** 제대로 계산된다.
  안 넘기면 모든 곤충을 날개 2장 기준으로 본다.

## ✅ 곤충 종류 6개 확정 (2026-09-18)

Jin 판단: *"사슴벌레를 세분화하기보다 곤충 종류를 늘리는 게 낫다고 봐"*
→ 사슴벌레 3종(왕·넓적·톱)을 **`사슴벌레` 하나로 합치고**, 그 자리에 사마귀·벌·나비를 넣었다.

| 종이·앱 표시 | key | 정상 날개 |
|---|---|---|
| 장수풍뎅이 | `rhino` | 2 |
| 사슴벌레 | `stag` | 2 |
| 사마귀 | `mantis` | 4 |
| 벌 | `bee` | 4 |
| 나비 | `butterfly` | 4 |
| 기타 곤충 | `other` | 2 |

목록은 `lib/species.ts` **한 곳**에만 있고 세 군데로 퍼진다:
`/print`(종이 마킹란) · `/upload`(앱 버튼) · `app/api/convert-insect`(AI 생김새 규칙).
종이와 앱이 어긋나면 부스에서 진행이 멈추므로 절대 따로 적지 말 것.

`INSECT_ANATOMY_RULES`에서 **"큰턱은 2개"를 뺐다.** 나비·벌에게도 큰턱을 그리라는 말이 되기 때문이다.
큰턱 개수는 사슴벌레 `anatomy` 안에서만 지정한다.

## ✅ 그림 꾸미기: 색깔 · 느낌 2줄 (2026-09-18 확정·구현 완료)

**왜 넣었나**: 아이가 금색 사슴벌레를 그렸는데 AI가 검은색으로 그려버리면 "내 거랑 다른데?"가 된다.
연필로만 그린 그림은 AI가 색을 알 수조차 없다. 종이에 2줄만 넣어 이 사고를 막는다.

목록은 `lib/appearance.ts` 한 곳에 있고, `/print`·`/upload`·`app/api/convert-insect` 세 군데로 퍼진다.

| 줄 | 선택지 |
|---|---|
| 색깔 | 검정 · 갈색 · 빨강 · 파랑 · 초록 · 보라 · 금색 · 무지개 |
| 느낌 | 무섭게 · 멋있게 · 귀엽게 · 빛나게 |

- **둘 다 안 골라도 된다.** 안 고르면 "아이 그림에 쓴 색을 최대한 그대로 살려라"가 프롬프트에 들어간다.
  앱에서는 `그림대로` / `그냥` 버튼이 기본 선택 상태다.
- **능력치에는 전혀 영향이 없다.** 배틀 밸런스를 건드리지 않으려고 일부러 그림만 바꾸게 했다.
- **DB에 저장하지 않는다.** 저장하려면 `insects`에 컬럼 2개를 더하는 마이그레이션이 필요한데,
  그걸 안 돌린 채 배포하면 곤충 저장이 통째로 막힌다. 행사 3주 전에 감수할 위험이 아니다.
  (나중에 "무슨 색이 인기였나"를 보고 싶으면 그때 컬럼을 추가하면 된다.)
- 색깔·느낌 문장은 프롬프트의 **"반드시 지켜야 할 규칙"** 쪽에 넣었다.
  위쪽 연출 문장에 두면 AI가 "멋있게"를 핑계로 무시한다.
- 기존 프롬프트에 하드코딩돼 있던 "박진감 넘치는 / 역동적인 포즈"는 빼고,
  느낌을 안 골랐을 때의 기본값(`DEFAULT_MOOD_PROMPT`)으로 옮겼다. 안 그러면 `귀엽게`와 충돌한다.

⚠️ **종이가 까맣게 인쇄되던 버그도 같이 잡았다.** `app/layout.tsx`의
`body className="bg-slate-950 text-slate-100"`이 우선순위에서 이겨서, `/print`의 `body { background: #fff }`가
먹지 않고 있었다. `html body { ... !important }`로 바꿨다. 종이를 뽑기 전에 발견해서 다행인 건이다.
**앞으로 `/print`를 고치면 실제로 화면을 띄워 흰 종이인지 눈으로 확인할 것.**

## ✅ 배틀 연출 강화 (2026-09-18 구현 완료)

사용자 요청 3가지를 한 번에 반영했다. **추가로 구매한 것은 없다. 전부 CSS/코드로 만들었다.**

**1) 상대를 랭킹에서 고르기** (`app/battle/page.tsx`)

기존에는 무작위 상대였다. 이제 준비 화면에서 랭킹 순 목록을 보고 직접 고른다.
- 목록은 **이미지를 빼고** 불러온다(`id, nickname, species, level, stats`만). 300명이 있어도 목록은 가볍다.
  고른 상대의 그림 한 장만 배틀 시작 때 따로 받아온다.
- 최고 점수순 정렬 · 이름 검색 · 20명씩 더 보기 · 🎲아무나 버튼.

**2) 때리고 맞는 이펙트** (`app/globals.css` 아래쪽 "배틀 연출" 블록)

곤충 그림은 AI가 만든 **정지 이미지 한 장**이라 팔다리를 따로 움직일 수 없다.
그래서 옛날 2D 게임 방식으로 **그림 전체**를 움직인다.
- 공격: 상대 쪽으로 몸을 날렸다 돌아옴(`lunge-down`/`lunge-up`)
- 피격: 빨갛게 번쩍 + 좌우 흔들림(`hit-flash`), 화면 전체 흔들림(`screen-shake`)
- 타격 순간: 충격파 고리 + 베기 자국 + 💥 + 깎인 수치가 떠오름
- `prefers-reduced-motion` 기기에서는 연출이 자동으로 꺼진다(어지러움 방지).

**3) 배틀당 1번 필살기** (`lib/special-moves.ts`)

⚠️ **아이가 필살기를 따로 고르지 않는다.** 곤충의 **가장 높은 능력치가 곧 필살기**다.
→ 종이 마킹란을 한 줄도 늘리지 않으면서 아이마다 다른 필살기가 나온다. (종이 레이아웃 재작업 불필요)

| 제일 높은 능력치 | 필살기 | 효과 |
|---|---|---|
| 공격력 | ⚔️ 큰턱 강타 | 내 점수 ×1.35 |
| 수비력 | 🛡️ 철갑 방어 | 상대 점수 ×0.74 |
| HP | 💚 불굴의 생명력 | 내 ×1.18, 상대 ×0.87 |
| 생존능력 | 🌫️ 그림자 숨기 | 내 ×1.10, 상대 ×0.81 |
| 지능 | 🔮 약점 간파 | 내 ×1.25, 상대 ×0.92 |

다섯 기술 모두 **점수 비율이 약 1.35배로 동일**하다. 어떤 게 나와도 손해가 아니고 연출만 다르다.
밸런스 숫자는 `lib/special-moves.ts` 한 파일에서만 고치면 된다.

**진행 흐름**: VS 연출 → 1라운드(탐색전) → **⚡필살기 버튼 4초** → 필살기 연출(화면 섬광·기술 이름·에너지 고리)
→ 마지막 공방 → 결과.

- 상대(AI)는 자기 필살기를 **항상** 쓴다. 안 그러면 내가 누르기만 하면 무조건 이긴다.
- 못 누르면 불리해지므로, 준비 화면에서 "⚡버튼이 뜨면 바로 눌러!"라고 미리 안내한다.
- **운·크리티컬은 배틀 시작 때 한 번만 굴려둔다**(`rollBattle`) → 필살기 선택 뒤 `resolveBattle`로 배수만 곱한다.
  선택할 때마다 다시 굴리면 "필살기를 썼는데 더 나빠졌다"가 생기기 때문이다.

**남은 것**: Jin이 직접 설계하겠다고 한 필살기 **이름**이 나오면 `lib/special-moves.ts`의
이름·설명·숫자만 바꾸면 된다. 구조는 그대로 쓸 수 있다. (2026-09-19 "내가 정해볼게")

**5) ⏱ 필살기 타이밍 판정 — 리듬게임 방식 (2026-09-19 추가, Jin 요청)**

Jin 표현: *"4초동안 큰 타이밍버튼이 작아지면서 2초정도에 제대로된 타이밍에 누르면 퍼펙트!"*
(이지투디제이·야구게임 파워게이지 방식. 참고로 준 유튜브 영상은 Claude가 볼 수 없어 설명만으로 만들었다.)

큰 흰 고리가 **4초 동안 1.8배 → 0.2배로 일정하게** 줄어들며, **정확히 2초에 1.0배**가 되어
가운데 목표 고리와 겹친다. 그 순간에 누르면 퍼펙트.

| 판정 | 오차 | 내 점수 |
|---|---|---|
| ✨ 퍼펙트! | ±250ms | ×1.35 |
| 🔥 그레이트! | ±550ms | ×1.20 |
| 👍 굿! | ±900ms | ×1.10 |
| 💥 발동! | 그 밖 | ×1.00 |

- **판정을 일부러 넉넉하게 잡았다.** 어른 기준(±100ms 등)으로 맞추면 아이들은 "발동!"만 보고
  타이밍 요소가 있는 줄도 모른다.
- 고리 애니메이션은 **반드시 `linear`**. ease 를 쓰면 속도가 변해서 아이가 타이밍을 예측할 수 없다.
- 고리는 **CSS가 그리고**(60fps), JS는 "눌렀나"만 본다. 매 프레임 setState 로 그리면
  폰에서 버벅여서 타이밍 게임 자체가 성립하지 않는다.
- 시간 재기는 `requestAnimationFrame` 2번 기다린 **뒤에** 시작한다. 바로 재면 React가 버튼을
  그리기 전이라 판정 시계가 아이가 보는 고리보다 앞서 간다.
- 타이밍 보너스는 기술 배수와 **따로**, 내 점수에만 곱한다(`resolveBattle`의 `timingA`).
  기술 배수 자체를 키우면 철갑 방어(상대를 깎는 기술)만 유독 세져서 5개 밸런스가 틀어진다.

**밸런스**: 둘 다 필살기 → 비김 / 내가 퍼펙트 → 1.35배 유리 / 버튼 놓침 → 0.74배 불리.

✅ **2초에 실제로 겹치는지 브라우저로 측정해 확인했다** (t=2000ms에 고리 104px = 목표 104px).
이 숫자(`PERFECT_AT_MS`, 고리 scale 범위, `CHANCE_MS`)를 고치면 **반드시 다시 측정할 것.**
셋 중 하나만 어긋나도 "겹쳤는데 퍼펙트가 안 뜬다"가 된다.

**4) 🏆 랭킹 도전 — 사다리 방식 (2026-09-18 추가, Jin 요청)**

배틀의 **기본 방식**이 되었다. 랭킹 **3위 → 2위 → 1위**를 차례로 올라간다.

- 이기면 위로, **지면 같은 상대에게 다시** 붙는다.
- 기회는 **총 3번**(`LADDER_BATTLES`). 다 쓰면 성적표가 뜬다.
- 도전 시작할 때 상위 3명을 **고정**한다. 도중에 다른 아이가 점수를 올려 순위가 바뀌어도
  사다리는 흔들리지 않는다.
- 상대가 3명보다 적으면 있는 만큼만 도전한다. (행사 초반 대비)
- 3번을 **강제로 막지는 않는다.** "처음부터 다시"를 누르면 새 도전이 시작된다.
  DB에 도전 횟수를 박아 잠그면, 버그 한 번에 아이가 체험을 통째로 못 하게 된다.
  베타에서 감수할 위험이 아니라고 보고 화면 안에서만 세도록 했다. 하드 제한이 필요하면 그때 걸면 된다.
- **친구를 직접 골라서 붙기**(기존 상대 고르기)는 지우지 않고 **접어뒀다.**
  친구끼리 붙고 싶어 하는 건 부스에서 실제로 나오는 요구다. 이쪽은 횟수 제한이 없다.

⚠️ **순위 번호는 "나를 포함한 전체 기준"이다 (같이 고친 것).**
처음에는 쿼리에서 내 곤충을 빼고(`.neq`) 순위를 매겨서, **내가 1등이면 2등이 "1위"로 표시**됐다.
지금은 전부 불러와 순위를 매긴 **다음에** 나만 걸러낸다. 그래서 내가 1등이면 사다리가
🥈🥉4위가 될 수 있는데, 그게 맞는 표시다.

## 시상 구조 (2026-09-17 확정)

**두 갈래 랭킹**이며, 두 번째는 아직 앱에 없는 기능임.

**2026-09-17 개편**: 1위 상품을 "본인 곤충 캐릭터 포스터 1p + 소형 피규어 제작·증정"으로
신설하고, 기존 1~3위 상품을 모두 한 단계씩 아래로 내렸다.

| 순위 | 배틀 랭킹 (앱이 자동 집계) | 예쁜 곤충 랭킹 (**그림**으로 심사 — 미구현) |
|---|---|---|
| 1위 | **내 곤충 포스터 1p + 소형 피규어 제작·증정** | **내 곤충 포스터 1p + 소형 피규어 제작·증정** |
| 2위 | 왕사슴벌레 사육세트 / 장수풍뎅이 사육세트 중 택1 | 왕사슴벌레 사육세트 / 장수풍뎅이 사육세트 중 택1 |
| 3위 | 사슴벌레 & 장수풍뎅이 채집 체험권 | 사슴벌레 & 장수풍뎅이 채집 체험권 |
| 4위 | 사슴벌레 애벌레 사육세트 | 그림그리기 세트 |

**4위까지 시상한다** (2026-09-17 사용자 확인). 기존 1~3위 상품을 2~4위로 내린 것이 의도한 바였음.
→ 한 랭킹당 4명 × 2개 랭킹 = **총 8명 시상**.

⚠️ 1위 상품은 **주문 제작**이라 리드타임이 있다. 발표(10/4) 후 제작·배송 일정을 별도로 잡아야 하고,
피규어 제작에 쓸 이미지 해상도가 충분한지도 확인이 필요하다.

**"예쁜 곤충 랭킹" 심사 방식 — 확정·구현 완료 (2026-09-18)**

**운영자가 직접 뽑되, 아이들 하트 수를 참고용으로 함께 본다.**
사용자 표현: *"하트 투표율이 높아도 운영자 픽은 다를 수도 있는 거지"* — 하트는 참고 지표일 뿐
순위를 자동으로 결정하지 않는다. 친구끼리 몰아주기를 걸러내기 위함.

- `/gallery` — 아이들이 친구 곤충을 구경하며 하트를 누르는 화면. 자기 곤충에는 못 누름.
  `hearts` 테이블의 기본키가 (insect_id, player_id)라 한 아이가 같은 곤충에 중복으로 못 누름.
- `/admin/judge` — 운영자 심사 화면. 격자로 훑어보며 ⭐후보 담기 → 1~4위 지정.
  하트순/최신순 정렬, 후보만 보기, 확정 순위 상단 고정. 같은 순위를 두 곤충이 가질 수 없음.
  환경변수 `ADMIN_CODE`로 암호를 걸어둠 (RLS가 꺼져 있어 데이터 보호용은 아니고, 아이들이
  실수로 들어가는 것을 막는 용도).
- 순위는 `insects.judge_rank`(1~4)에 저장됨.
- **이미지가 무거워서 두 화면 모두 조금씩 끊어 불러온다** (갤러리 12마리, 심사 24마리씩).
  300명분 base64 이미지를 한 번에 불러오면 폰이 멈춘다.

**시상 운영 방식 (2026-09-17 확정)**

- **2일 통합 집계**, 발표는 10/4 행사 종료 시점. → 날짜별 랭킹 분리 불필요.
- **발표는 카카오톡 채널로만** 한다. "채널을 추가해야 결과를 볼 수 있다"고 안내해
  채널 구독자 모집을 겸하는 것이 사용자의 의도.
- **선물은 집으로 배송**. 당일 참여한 아이에게는 현장에서 별도의 소소한 선물
  (애벌레집, 간단한 사육 물품)을 따로 지급 — 이건 앱과 무관한 현장 운영 사항.

**배송 주소를 앱에서 받지 않는다 (중요)**

300명 전원의 주소를 앱에서 받으면 아동 개인정보 부담이 크고 부스 회전율도 떨어진다.
대신 **당첨자에게만 카카오 채널 1:1 대화로 주소를 받는다.**

연결 고리는 종이 번호다. 마지막 화면에서 아이에게
"카카오 채널을 추가하고 **내 번호(예: A-014)를 채팅으로 보내달라**"고 안내한다.
그러면 채널 대화방에 번호가 남아, 당첨 번호의 주인에게 바로 연락할 수 있다.
→ 앱이 수집하는 개인정보는 **0**이 되고, 채널 모집·당첨자 특정이 한 번에 해결된다.

운영 팁: 현장 참여 선물(애벌레집 등)을 줄 때 "채널 추가하고 번호 보냈는지" 확인하고
지급하면 전환율이 크게 올라간다.

- **구글 시트 리스트업**: 아이들이 처음 고른 설정값 + 받고 싶은 선물 + 설문 응답을
  구글 시트에 실시간으로 쌓기. **Apps Script 웹앱 방식으로 확정** (2026-09-17).
  - 전송 코드는 작성 완료: `google-apps-script/sheet-logger.gs`(시트에 붙여넣는 스크립트),
    `app/api/log-sheet/route.ts`(서버 중계), `lib/sheet-log.ts`(화면에서 호출하는 헬퍼)
  - 환경변수 `SHEETS_WEBHOOK_URL` / `SHEETS_WEBHOOK_TOKEN` 두 개가 있어야 동작.
    **비워두면 기록만 건너뛰고 앱은 정상 동작**하므로 미설정 상태로 배포해도 안전함.
  - 헤더는 자동 확장됨. 설문 문항이 늘어도 시트를 다시 만들 필요 없음.
  - **아직 화면에 연결하지 않음.** 소유권 방식 / 선물 목록 / 설문 문항이 확정된 뒤
    `logToSheet({ ... })` 한 줄을 시작 화면에 붙이면 됨.

## 핵심 미해결 문제: 곤충 소유권 인증

**왜 필요한가**: 배틀 랭킹에 따라 실제 선물을 지급하므로, "이 곤충이 내 것"이라는 증명이 필요함.

**사용자가 우려한 점** (2026-09-17):
- 비밀번호 방식 → 아이가 잊어버림
- 비밀번호 방식 → 남이 추측해서 남의 곤충을 자기 것이라고 주장할 수 있음

**기존 시도**: 구글/이메일 매직링크 로그인 도입 (`lib/auth.ts`, `app/auth/finish/`, RLS 정책).
문제 인식은 맞았으나 부스 환경에 부적합 — 아이들은 구글 계정이 없고, 매직링크는 메일함을 열어야 함.
Supabase 기본 메일 발송은 건수 제한도 낮음.

**✅ 해결 방식 확정 및 구현 완료 (2026-09-17): 종이 참가 번호**

A4 그림 용지 위쪽에 고유 번호(예: `A-014`)와 QR을 미리 인쇄한다. 그 종이가 곧 신분증이다.

- 번호를 *정하는* 게 아니라 *받는* 것이므로 **잊어버릴 수 없다**
- 시상 때 아이가 그 종이를 들고 오므로, 번호만 훔쳐봐도 **상품을 받을 수 없다**
- QR을 찍으면 `/start?t=A-014` 로 열리며 번호가 자동 입력된다 (오타·타이핑 0)
- 같은 번호로 다시 들어오면 원래 곤충으로 이어진다 (`findPlayerByTicket`)

관련 파일: `lib/ticket.ts`(번호 규칙), `lib/session.ts`(localStorage 기반 세션),
`app/start/page.tsx`(시작 화면), `lib/survey.ts`(선물·설문 목록 한곳 관리).

**구글/이메일 로그인 코드(`lib/auth.ts`, `app/auth/finish/`)는 지우지 않고 남겨둠.**
나중에 부모님 마케팅 동의 수집에 쓸 수 있음. `/signup`은 `/start`로 넘겨주는 페이지가 됨.

⚠️ **Supabase에서 마이그레이션을 실행해야 동작한다.** 로그인이 없어져 `auth.uid()` 기반
RLS 정책이 모든 저장을 막으므로, `supabase/schema.sql` 맨 아래 2026-09-17 블록을
SQL Editor에서 실행해 정책을 걷어내고 `ticket_code` / `prize_choice` / `survey` 컬럼을 추가할 것.

## 아직 안 한 작업

1. ~~**Vercel 배포**~~ — ✅ 완료 (2026-09-17). 고정 주소 `https://g7bb.vercel.app`
   - GitHub `main`에 푸시하면 1~2분 뒤 자동 반영됨
   - 환경변수 5개는 Vercel에 등록 완료
   - **Supabase Auth의 Site URL / Redirect URL 등록은 아직** (로그인 방식 확정 후)
2. **OMR 종이 마킹 자동 인식** — 아이들이 종이에 동그라미로 표시한 항목(곤충종류/성장단계/출신지/7부위 점수)을
   사진으로 찍으면 Gemini가 읽어서 JSON으로 변환하는 기능. 현재는 앱 화면에서 직접 입력하는 방식.
   손글씨는 오타·악필 위험이 커서 **동그라미 마킹 방식**으로 가기로 결정함.
3. 실제 아이 사진/그림으로 최종 확인

## 주의사항 (함정 모음)

- **`.env.local`은 깃에 없음** (보안상 제외). 새 컴퓨터에서 작업하려면 `.env.local.example`을 복사해
  Gemini API 키, Supabase URL/anon key, 카카오 채널 링크를 채워야 함.
- **Gemini 모델명은 자주 바뀜**. 이미지 변환이 실패하면 `GEMINI_IMAGE_MODEL` 환경변수부터 최신 모델명인지 확인.
  이미지 생성 모델은 무료 등급 할당량이 0이라 **결제 등록이 필수**.
- **Supabase RLS는 꺼져 있음** (베타 편의). 3일짜리 행사용으로는 충분하지만 정식 운영 시 정책 추가 필요.
  행사 종료 후 `supabase/schema.sql` 하단의 truncate 명령으로 아이들 데이터 정리 권장.
- **스키마 변경 시 마이그레이션 필요**. `create table if not exists`는 기존 테이블에 컬럼을 추가하지 않음.
  `supabase/schema.sql`의 `alter table ... add column if not exists` 블록을 SQL Editor에서 실행해야 함.
- **원본 사진은 저장하지 않음** (아동 개인정보 최소화). 변환된 곤충 이미지만 저장.
- **배틀 계산에서 `NaN` 주의**. 예전 스탯 형식(hp/atk/def/spd)의 데이터가 섞이면 계산이 깨짐.
  스탯 구조를 바꿀 때는 기존 `insects` 행을 정리할 것.
- **PowerShell로 Supabase에 한글 데이터를 직접 넣지 말 것**. 인코딩이 깨져 `???`로 저장됨.
  테스트 데이터도 브라우저(JS)를 통해 넣거나 UTF-8 바이트로 명시 변환해야 함.

## 개발 환경 메모

- Windows / PowerShell. `npm run dev` → http://localhost:3000
- 여러 컴퓨터를 오가며 작업함. **작업 시작 전 `git pull`, 작업 후 커밋+푸시**가 유일한 동기화 수단
  (OneDrive 경로에 있지만 실제로 OneDrive 동기화는 사용하지 않음).
