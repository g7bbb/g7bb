# 곤충 배틀 베타 (곤충진화 ai)

## 이 프로젝트가 뭔지

곤충체험 사업에서 쓸 체험용 웹앱의 베타 버전. 아이들이 A4 용지에 곤충을 그리고 필요한 정보를 표시한 뒤,
그 종이를 사진으로 찍어 앱에 올리면 → AI가 멋진 곤충 이미지로 변환 → 다른 아이 곤충과 환경별 생존 배틀 → 랭킹 확인.

**일정**: 9월 말까지 기능 완성 → **10월 3~4일(2일간)** 대전 곤충행사에서 현장 테스트.
**예상 인원**: 2일간 약 300명.
**목표 수준**: 디자인 완성도보다 "체험을 실제로 한두 번 돌려볼 수 있는" 베타 퀄리티.

## 작업 순서 (2026-09-17 확정) — 이 순서대로만 진행할 것

작업이 뒤죽박죽 진행된다는 지적이 있었음. **아래 순서를 벗어나지 말 것.**
새 아이디어가 나오면 "사용자 요청 사항"에 적어만 두고, 순서는 건드리지 않는다.

| # | 작업 | 담당 | 기한 | 상태 |
|---|---|---|---|---|
| 0 | Supabase 마이그레이션 실행 | 사장님 | 즉시 | ✅ 완료 (2026-09-17, ticket/survey + mutations 둘 다) |
| 1 | g7bb.vercel.app 폰으로 테스트 | 사장님 | 즉시 | 🔄 진행 중 |
| 2 | 종이 레이아웃 확정 (곤충 종류 / 출신지 항목) | 사장님 | 9/18 | ⬜ |
| 3 | 인쇄용 PDF 제작 (400장, 번호+QR) | Claude | 9/18 | ⬜ |
| 4 | **인쇄 발주** | 사장님 | 9/19 | ⬜ |
| 5 | 구글시트 Apps Script 설정 | 사장님 | 9/21 | ⬜ |
| 6 | 예쁜 곤충 심사 관리자 화면 | Claude | 9/23 | ⬜ |
| 7 | OMR 마킹 자동 인식 | Claude | 9/27 | ⬜ |
| 8 | 실물 종이로 처음부터 끝까지 통합 테스트 | 둘 다 | 9/30 | ⬜ |
| 9 | 예비일 (밀린 것 정리) | | 10/1~2 | |

**순서를 이렇게 잡은 이유**

- **0~1번이 최우선**: 마이그레이션 전에는 앱이 아무것도 저장하지 못한다. 지금 막혀 있는 상태.
- **2~4번(인쇄)이 그다음**: 인쇄소는 리드타임이 있다. 종이가 늦으면 다른 게 다 준비돼도 행사가 불가능하다.
  **이번 주 안에 발주가 나가야 한다.**
- **7번(OMR)이 마지막인 이유**: 실패해도 앱에서 직접 입력하면 체험은 돌아간다. 유일하게 포기 가능한 항목.

## 사용자와 협업하는 방식 (중요)

- 사용자는 개발 지식이 거의 없음. **콘텐츠·방향·결과에 대한 피드백**을 담당.
- **개발 결정과 효율적인 방법은 Claude가 정하고, 초보자 눈높이의 한국어로 설명**할 것.
- 계정 생성·로그인·결제처럼 사용자 본인이 해야 하는 일은 클릭 순서까지 구체적으로 안내.
- 게임 밸런스·이미지 스타일 같은 콘텐츠 결정은 기본안을 제시하고 피드백을 받는 방식.

## 기술 스택과 선택 이유

| 기술 | 이유 |
|---|---|
| Next.js 14 (App Router) + TypeScript | 화면과 서버 기능을 한 프로젝트에서 처리. 별도 백엔드 서버 불필요 |
| Vercel | 배포 대상 (아직 미배포). GitHub 연동만 하면 자동 배포 |
| Supabase | 아이디/곤충/배틀 기록 저장. 브라우저에서 직접 호출하는 단순 구조 |
| Google Gemini 이미지 API | 그림 → 곤충 이미지 변환. **서버(API route)에서만 호출**해 키 노출 방지 |
| Tailwind CSS | 최소한의 스타일링으로 빠르게 모바일 화면 구성 |

## 4가지 핵심 기능 상태

1. **사진 → 곤충 이미지 변환** — 작동 확인 완료 (`app/api/convert-insect/route.ts`)
2. **환경별 생존 배틀** — 작동 확인 완료. VS 화면, 타격 연출, HP 바, 크리티컬 포함
3. **아이디 생성 / 랭킹** — 작동 확인 완료
4. **카카오톡 채널 링크** — 코드는 있음. `NEXT_PUBLIC_KAKAO_CHANNEL_URL` 값만 넣으면 됨

## 게임 시스템 설계 (사용자가 직접 정한 규칙)

**5개 핵심 능력치**: 공격력(atk) · 수비력(def) · HP(hp) · 생존능력(surv) · 지능(int)

**7개 신체부위(각 1~5점) → 능력치 매핑** (`lib/insect-stats.ts`):

| 부위 | 연결되는 능력치 |
|---|---|
| 머리(턱) | 공격력, 지능 |
| 다리(발톱) | 공격력, 생존능력 |
| 갑옷(두께) | HP, 수비력 |
| 날개 | 수비력, 생존능력 |
| 눈 | 수비력, 생존능력 |
| 육감 | 공격력, 수비력, 생존능력, 지능 |
| 유전 | 공격력, 수비력, 생존능력 |

각 능력치는 기여하는 부위 점수 합을 0~100으로 정규화. **수비력만 성장단계 보정을 받지 않음.**

**성장단계**: 신생충(0.9배) → 1년충(1.0배) → n년충(1.15배). 단계가 높을수록 경험치도 더 많이 획득.

**환경별 가중치** (`lib/battle-engine.ts`) — 같은 곤충도 환경에 따라 결과가 달라지도록 Claude가 설계한 부분:
운석충돌(생존·수비·HP) / 기온상승(생존·지능·수비) / 저지대(공격·수비·생존) / 물근처(생존·수비·HP) / 고지대(지능·생존·HP)

**레벨/크리티컬**: XP 50당 1레벨. 레벨당 능력치 +3%, 크리티컬 확률 +2%(기본 10%, 최대 50%). 크리티컬 시 점수 1.5배.

밸런스 조정이 필요하면 위 두 파일의 숫자만 바꾸면 됨.

## 사용자 요청 사항 (기억할 것)

- **시작 시 선물 선택 기능**: 아이가 게임을 처음 시작할 때 "받고 싶은 선물"을 고르게 하는 옵션.
  랭킹 상위 입상 시 그 선물을 지급하는 구조. 동기부여 + 어떤 선물이 인기인지 실시간 파악 용도.
  → `players`에 `prize_choice` 컬럼 추가 필요.

- **간단한 설문** (문항 확정됨 2026-09-17, 형식은 선택형 권장안 제시 후 승인 대기):
  1. 제일 좋아하는 곤충 2. 선물 중 받고 싶은 상품 3. 채집 체험 해보고 싶은지
  4. AI 곤충배틀 게임이 나오면 해보고 싶은지
  → 2번은 위 "선물 선택 기능"과 같은 항목이므로 하나로 합칠 것.

## 검토 중: 상상 부위에 대한 보너스/페널티 (2026-09-17 제기, 사용자가 고민 중)

**배경**: 생성된 왕사슴벌레의 큰턱이 4개로 나오는 오류가 있어, 종별 해부학 규칙을
프롬프트에 넣어 실제 생김새대로 그리도록 고쳤다(`lib/species.ts`).

**그런데 사용자가 뒤집어 생각함**: 이 체험은 교육 목적의 도감이 아니다.
아이가 상상으로 턱 4개, 다리 8개를 그릴 수 있고, **그걸 AI가 "고쳐"버리면
"내가 그린 거랑 다르다"가 되어 체험의 핵심(내 그림이 살아나는 것)이 깨진다.**

**검토 방향**: 실제와 다르게 그린 부위를 그대로 살리되, 게임적으로 **주고받기**를 준다.
많이 달수록 세지지만 그만큼 잃는 것도 있게 해서 "진화는 공짜가 아니다"를 체감시킨다.

⚠️ **이 결정은 종이 인쇄 전에 나야 한다.** 아이가 "턱 4개"를 표시할 마킹란이
종이에 있어야 하기 때문. 순서표의 2번(종이 레이아웃 확정)에 포함되는 사안임.

현재 배포된 상태는 **실제 해부학 강제**. 상상 허용으로 바꾸려면
`lib/species.ts`의 `INSECT_ANATOMY_RULES`와 프롬프트 문구만 고치면 되므로 되돌리기 쉽다.

## 시상 구조 (2026-09-17 확정)

**두 갈래 랭킹**이며, 두 번째는 아직 앱에 없는 기능임.

**2026-09-17 개편**: 1위 상품을 "본인 곤충 캐릭터 포스터 1p + 소형 피규어 제작·증정"으로
신설하고, 기존 1~3위 상품을 모두 한 단계씩 아래로 내렸다.

| 순위 | 배틀 랭킹 (앱이 자동 집계) | 예쁜 곤충 랭킹 (**그림**으로 심사 — 미구현) |
|---|---|---|
| 1위 | **내 곤충 포스터 1p + 소형 피규어 제작·증정** | **내 곤충 포스터 1p + 소형 피규어 제작·증정** |
| 2위 | 왕사슴벌레 사육세트 / 장수풍뎅이 사육세트 중 택1 | 왕사슴벌레 사육세트 / 장수풍뎅이 사육세트 중 택1 |
| 3위 | 사슴벌레 & 장수풍뎅이 채집 체험권 | 사슴벌레 & 장수풍뎅이 채집 체험권 |
| 4위 | 사슴벌레 애벌레 사육세트 | 그림그리기 세트 |

⚠️ 4위 시상 여부는 미확인. "모두 한 단계씩 내려달라"는 요청을 그대로 적용해 기존 3위 상품이
4위로 내려간 상태임. 4위를 시상하지 않을 거라면 그 줄을 지우면 된다.

⚠️ 1위 상품은 **주문 제작**이라 리드타임이 있다. 발표(10/4) 후 제작·배송 일정을 별도로 잡아야 하고,
피규어 제작에 쓸 이미지 해상도가 충분한지도 확인이 필요하다.

- **"예쁜 곤충 랭킹"은 아이가 직접 그린 그림으로 판단**한다고 사용자가 정함.
  심사 주체(운영자 심사 / 아이들 투표 / AI)는 미정 — 운영자 심사 + 하트 투표 보조안을 제안해둠.

**시상 운영 방식 (2026-09-17 확정)**

- **2일 통합 집계**, 발표는 10/4 행사 종료 시점. → 날짜별 랭킹 분리 불필요.
- **발표는 카카오톡 채널로만** 한다. "채널을 추가해야 결과를 볼 수 있다"고 안내해
  채널 구독자 모집을 겸하는 것이 사용자의 의도.
- **선물은 집으로 배송**. 당일 참여한 아이에게는 현장에서 별도의 소소한 선물
  (애벌레집, 간단한 사육 물품)을 따로 지급 — 이건 앱과 무관한 현장 운영 사항.

**배송 주소를 앱에서 받지 않는다 (중요)**

300명 전원의 주소를 앱에서 받으면 아동 개인정보 부담이 크고 부스 회전율도 떨어진다.
대신 **당첨자에게만 카카오 채널 1:1 대화로 주소를 받는다.**

연결 고리는 종이 번호다. 마지막 화면에서 아이에게
"카카오 채널을 추가하고 **내 번호(예: A-014)를 채팅으로 보내달라**"고 안내한다.
그러면 채널 대화방에 번호가 남아, 당첨 번호의 주인에게 바로 연락할 수 있다.
→ 앱이 수집하는 개인정보는 **0**이 되고, 채널 모집·당첨자 특정이 한 번에 해결된다.

운영 팁: 현장 참여 선물(애벌레집 등)을 줄 때 "채널 추가하고 번호 보냈는지" 확인하고
지급하면 전환율이 크게 올라간다.

- **구글 시트 리스트업**: 아이들이 처음 고른 설정값 + 받고 싶은 선물 + 설문 응답을
  구글 시트에 실시간으로 쌓기. **Apps Script 웹앱 방식으로 확정** (2026-09-17).
  - 전송 코드는 작성 완료: `google-apps-script/sheet-logger.gs`(시트에 붙여넣는 스크립트),
    `app/api/log-sheet/route.ts`(서버 중계), `lib/sheet-log.ts`(화면에서 호출하는 헬퍼)
  - 환경변수 `SHEETS_WEBHOOK_URL` / `SHEETS_WEBHOOK_TOKEN` 두 개가 있어야 동작.
    **비워두면 기록만 건너뛰고 앱은 정상 동작**하므로 미설정 상태로 배포해도 안전함.
  - 헤더는 자동 확장됨. 설문 문항이 늘어도 시트를 다시 만들 필요 없음.
  - **아직 화면에 연결하지 않음.** 소유권 방식 / 선물 목록 / 설문 문항이 확정된 뒤
    `logToSheet({ ... })` 한 줄을 시작 화면에 붙이면 됨.

## 핵심 미해결 문제: 곤충 소유권 인증

**왜 필요한가**: 배틀 랭킹에 따라 실제 선물을 지급하므로, "이 곤충이 내 것"이라는 증명이 필요함.

**사용자가 우려한 점** (2026-09-17):
- 비밀번호 방식 → 아이가 잊어버림
- 비밀번호 방식 → 남이 추측해서 남의 곤충을 자기 것이라고 주장할 수 있음

**기존 시도**: 구글/이메일 매직링크 로그인 도입 (`lib/auth.ts`, `app/auth/finish/`, RLS 정책).
문제 인식은 맞았으나 부스 환경에 부적합 — 아이들은 구글 계정이 없고, 매직링크는 메일함을 열어야 함.
Supabase 기본 메일 발송은 건수 제한도 낮음.

**✅ 해결 방식 확정 및 구현 완료 (2026-09-17): 종이 참가 번호**

A4 그림 용지 위쪽에 고유 번호(예: `A-014`)와 QR을 미리 인쇄한다. 그 종이가 곧 신분증이다.

- 번호를 *정하는* 게 아니라 *받는* 것이므로 **잊어버릴 수 없다**
- 시상 때 아이가 그 종이를 들고 오므로, 번호만 훔쳐봐도 **상품을 받을 수 없다**
- QR을 찍으면 `/start?t=A-014` 로 열리며 번호가 자동 입력된다 (오타·타이핑 0)
- 같은 번호로 다시 들어오면 원래 곤충으로 이어진다 (`findPlayerByTicket`)

관련 파일: `lib/ticket.ts`(번호 규칙), `lib/session.ts`(localStorage 기반 세션),
`app/start/page.tsx`(시작 화면), `lib/survey.ts`(선물·설문 목록 한곳 관리).

**구글/이메일 로그인 코드(`lib/auth.ts`, `app/auth/finish/`)는 지우지 않고 남겨둠.**
나중에 부모님 마케팅 동의 수집에 쓸 수 있음. `/signup`은 `/start`로 넘겨주는 페이지가 됨.

⚠️ **Supabase에서 마이그레이션을 실행해야 동작한다.** 로그인이 없어져 `auth.uid()` 기반
RLS 정책이 모든 저장을 막으므로, `supabase/schema.sql` 맨 아래 2026-09-17 블록을
SQL Editor에서 실행해 정책을 걷어내고 `ticket_code` / `prize_choice` / `survey` 컬럼을 추가할 것.

## 아직 안 한 작업

1. ~~**Vercel 배포**~~ — ✅ 완료 (2026-09-17). 고정 주소 `https://g7bb.vercel.app`
   - GitHub `main`에 푸시하면 1~2분 뒤 자동 반영됨
   - 환경변수 5개는 Vercel에 등록 완료
   - **Supabase Auth의 Site URL / Redirect URL 등록은 아직** (로그인 방식 확정 후)
2. **OMR 종이 마킹 자동 인식** — 아이들이 종이에 동그라미로 표시한 항목(곤충종류/성장단계/출신지/7부위 점수)을
   사진으로 찍으면 Gemini가 읽어서 JSON으로 변환하는 기능. 현재는 앱 화면에서 직접 입력하는 방식.
   손글씨는 오타·악필 위험이 커서 **동그라미 마킹 방식**으로 가기로 결정함.
3. 실제 아이 사진/그림으로 최종 확인

## 주의사항 (함정 모음)

- **`.env.local`은 깃에 없음** (보안상 제외). 새 컴퓨터에서 작업하려면 `.env.local.example`을 복사해
  Gemini API 키, Supabase URL/anon key, 카카오 채널 링크를 채워야 함.
- **Gemini 모델명은 자주 바뀜**. 이미지 변환이 실패하면 `GEMINI_IMAGE_MODEL` 환경변수부터 최신 모델명인지 확인.
  이미지 생성 모델은 무료 등급 할당량이 0이라 **결제 등록이 필수**.
- **Supabase RLS는 꺼져 있음** (베타 편의). 3일짜리 행사용으로는 충분하지만 정식 운영 시 정책 추가 필요.
  행사 종료 후 `supabase/schema.sql` 하단의 truncate 명령으로 아이들 데이터 정리 권장.
- **스키마 변경 시 마이그레이션 필요**. `create table if not exists`는 기존 테이블에 컬럼을 추가하지 않음.
  `supabase/schema.sql`의 `alter table ... add column if not exists` 블록을 SQL Editor에서 실행해야 함.
- **원본 사진은 저장하지 않음** (아동 개인정보 최소화). 변환된 곤충 이미지만 저장.
- **배틀 계산에서 `NaN` 주의**. 예전 스탯 형식(hp/atk/def/spd)의 데이터가 섞이면 계산이 깨짐.
  스탯 구조를 바꿀 때는 기존 `insects` 행을 정리할 것.
- **PowerShell로 Supabase에 한글 데이터를 직접 넣지 말 것**. 인코딩이 깨져 `???`로 저장됨.
  테스트 데이터도 브라우저(JS)를 통해 넣거나 UTF-8 바이트로 명시 변환해야 함.

## 개발 환경 메모

- Windows / PowerShell. `npm run dev` → http://localhost:3000
- 여러 컴퓨터를 오가며 작업함. **작업 시작 전 `git pull`, 작업 후 커밋+푸시**가 유일한 동기화 수단
  (OneDrive 경로에 있지만 실제로 OneDrive 동기화는 사용하지 않음).
